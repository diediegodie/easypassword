[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: Tests that require external services (PostgreSQL/Redis)
    unit: Fast tests with no external dependencies
asyncio_mode = auto
