import { Component } from '@angular/core';

@Component({
  selector: 'app-protected-placeholder',
  template: `
    <section class="protected-placeholder">
      <h2>Protected Area (placeholder)</h2>
      <p>This component is a placeholder for future protected UI.</p>
    </section>
  `,
})
export class ProtectedComponent {}
